namespace MnohonasobnePrirazeni3
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // POUZE TEORIE!
            // Mnohonasobna deklarace.
            ///bool b, b1, b2, b3;

            // Mnohonasobne přiřazení.
            /// b = b1 = true;
            /// b2 = b3 = false;

            // ZACATEK PROGRAMU.
            // Práce s podmínkami.
            // bool b;
            // b = false;

            // Prace s porovnavacími znaky
            // <, >, =>, <=, ...

            Console.WriteLine("V kódu definováno číslo, které je 5 >= 3 a pod ním je cyklus if.");
            bool b = 5 >= 3;

            if (b)
            {
                Console.WriteLine("Pět je větší nebo rovno třem.");
            }


            Console.ReadKey();
        }
    }
}
